## 背景
- 2024年に発生した能登半島地震による地形変化を地震前と地震後のDEMをもとに分析して、地形断面図として表現したい
- さらに、2024年8月の奥能登豪雨による地形変化も、断面図を比較することで表現したい。

## 作成したいアプリのイメージ
- 複数枚のDEMを指定して、端点のxy座標、インターバル距離を与えることで、端点を結ぶ側線について、指定したインターバルの間隔でxyzの値を取得してテーブル(データフレーム)を作成する。
- 作成したデータフレームをもとに、開始点からの距離をx軸に、z(標高)をy軸にプロットした折れ線グラフで断面図を表現する。
- DEM毎に線の色を変える。
- DEMのファイル名を凡例としてグラフと重ならないように表示する。
- 座標はDEMの空間参照と同一であることを確認する。今回は、EPSG: 6675とする。
- 作成された断面図には日本語表記が可能なものにする。
- まずは、ローカルで実行可能なアプリをPythonで記述し、問題がないことが確認できれば、githubpages 上で運用可能なwebアプリとして公開できるようにする。

## 作業の流れ
- mdを読み計画を立てる。
- 開発を実行する。
- 開発の結果をmdファイルに取りまとめる。