# 使い方ガイド

このドキュメントは、DEM断面図ツールの実際の操作方法をまとめたもの。背景・要件は
[`task01.md`](task01.md)、元バージョンの開発記録は [`task01_results.md`](task01_results.md)、
アーキテクチャの詳細は [`CLAUDE.md`](CLAUDE.md) を参照。

このツールには、Pythonのローカルアプリ(`src/dem_profile/`)とブラウザ版
(`docs/`、GitHub Pages公開可能)の2種類がある。どちらも「複数のDEM(GeoTIFF)から
側線に沿った標高値を抽出し、地形断面図として比較表示する」という同じ機能を持つ。

## セットアップ

このプロジェクトは [`uv`](https://docs.astral.sh/uv/) で管理している。システムに
Python本体が入っていなくても、`uv`が必要なバージョンのPythonを自動で取得する。

```
uv sync
```

DEM(GeoTIFF、EPSG:6675)は `dem/` ディレクトリなどに自分で配置しておく。

## Pythonローカルアプリ

3つの実行方法がある。用途に応じて使い分ける。

| コマンド | 側線の指定方法 | DEMパスの指定方法 |
| --- | --- | --- |
| `dem-profile` | 座標を直接指定 | コマンドライン引数 |
| `dem-profile-pick` | 陰影図をクリックして選択 | コマンドライン引数 |
| `dem-profile-pick-gui` | 陰影図をクリックして選択 | **起動後のGUIフォームで入力** |

側線の座標があらかじめ分かっている場合は `dem-profile`、地図を見ながら側線を選びたい
場合は `dem-profile-pick-gui` を使うのがおすすめ。

### 1. 座標を直接指定する(`dem-profile`)

```
uv run python -m dem_profile.cli \
  --dem dem/bfeq_pref_07ed694_67ee703.tif \
  --dem dem/afeq_mliti_07ed694_67ee703.tif \
  --dem dem/afst_07ed694_67ee703.tif \
  --start -1500 158000 --end 1500 158500 --interval 5 \
  --out-csv out/profile.csv --out-png out/profile.png
```

- `--dem`: DEM(GeoTIFF)のパス。複数回指定して重ねて比較できる。
- `--start` / `--end`: 側線の始点・終点座標(EPSG:6675)。
- `--interval`: サンプリング間隔(m)。
- `--out-csv`: 抽出したテーブルの出力先CSVパス(任意)。
- `--out-png`: 断面図PNGの出力先パス。

側線の座標が確定していればそのまま保存される(GUI操作なし、対話不要)。

### 2. 陰影図をクリックして側線を選ぶ(`dem-profile-pick`)

DEMパスをコマンドライン引数で指定してから、画面上のクリックで側線を選ぶ。

```
uv run python -m dem_profile.pick_transect \
  --hillshade-dem dem/afeq_mliti_07ed694_67ee703.tif \
  --dem dem/bfeq_pref_07ed694_67ee703.tif \
  --dem dem/afeq_mliti_07ed694_67ee703.tif \
  --dem dem/afst_07ed694_67ee703.tif \
  --interval 5 \
  --out-csv out/profile.csv --out-png out/profile.png
```

- `--hillshade-dem`: 陰影図の作成・表示に使うDEM(この例では震災後のDEM)。
- `--dem` を省略すると `--hillshade-dem` のみで断面図を作る。

実行するとウィンドウが1つ開き、**上に陰影図・下に断面図パネル**が表示される
(操作方法は次の項と共通)。

### 3. GUIフォームでDEMパスを入力する(`dem-profile-pick-gui`)

コマンドライン引数なしで起動でき、DEMパスなどは起動後のフォームで入力する。

```
uv run python -m dem_profile.pick_transect_gui
```

#### 手順1: DEM設定フォーム

起動すると「側線ピッカー - DEM設定」ウィンドウが開く。

1. DEMパスの行に、断面図に使うDEM(GeoTIFF)のパスを入力する(「参照...」ボタンで
   ファイル選択ダイアログからも選べる)。
2. 「+ DEMを追加」で行を増やせる。各行の「削除」ボタンで行を消せる(最低1行は必要)。
3. 各行の「陰影図」ラジオボタンで、陰影図の表示・クリック用に使うDEMを1つ選ぶ
   (通常は最新のDEM、例えば震災後のDEMを選ぶ)。
4. 各行の「凡例名」欄に、断面図の凡例に表示したい名前を入力できる(任意)。
   空欄の場合はDEMのファイル名がそのまま凡例名になる(例:「震災前」「震災後」など、
   わかりやすい名前を付けられる)。
5. 「サンプリング間隔 (m)」・「出力CSV」・「出力PNG」を確認・入力する
   (「参照...」ボタンで保存先を選ぶこともできる)。
6. 「陰影図を開く >>」を押す。

#### 手順2: 陰影図クリック・断面図確認・保存

フォームを閉じると、陰影図(上)と断面図パネル(下)を1つにまとめたウィンドウが開く。

- 陰影図の上で**2点クリック**すると、側線が確定し、**画面を切り替えることなく**
  下の断面図パネルにその場で断面図が表示される。
- **3点目をクリック**すると選び直しになる(赤い線と下の断面図がリセットされ、
  新しい1点目として扱われる)。
- **Escキー**でも選択をクリアできる。
- 気に入った断面図が表示されたら、右下の「**保存**」ボタンを押すと、その時点の
  断面図がCSV・PNGとして指定パスに書き出される。
- 保存ボタンは何度でも押し直せる(側線を選び直して再度保存すると、同じ出力先が
  上書きされる)。
- 保存ボタンを押さずにウィンドウを閉じた場合は、何もファイルに保存されない。

### 別PCで側線ピッカーを使う

デスクトップの「側線ピッカー」を別のPCでも使いたい場合、`.venv`やDEM本体(数百MB〜
1GB超)を除いた、ソースコード・セットアップスクリプトだけの軽量なzipを配って
セットアップできる。

1. **配布用zipを作る**(元のPC側)。`src/`・`tests/`・`pyproject.toml`・`uv.lock`・
   `.python-version`・各種`.md`・`install.ps1`だけを含む数百KB程度のzipを作る
   (`dem/`の実データやWeb版の`docs/`は含めない)。
2. **別PCにzipを展開する**。
3. **展開したフォルダで `install.ps1` を実行する**。

   ```
   powershell -ExecutionPolicy Bypass -File install.ps1
   ```

   `uv`が無ければ自動インストールし、`uv sync`でPython環境を構築し、デスクトップに
   「側線ピッカー」ショートカットを作成する(`.bat`経由だと組織管理PCのセキュリティ
   ソフトにブロックされることがあったため、`uv.exe`を直接呼び出す構成にしている)。
4. **DEM(GeoTIFF、EPSG:6675)ファイルを展開先の `dem\` フォルダに配置する**
   (元のPCから別途コピーする必要がある。zipには含まれていない)。
5. デスクトップの「側線ピッカー」を起動して動作確認する。

PowerShellスクリプトの実行がポリシーでブロックされる場合は、`install.ps1`の中身を
見ながら「`uv`をインストール→プロジェクトフォルダで`uv sync`→デスクトップに
`uv.exe`を対象としたショートカット(引数: `run python -m dem_profile.pick_transect_gui`)
を作成」を手動で行う。

## Web版(ブラウザ、`docs/`)

ビルド不要のプレーンなHTML/CSS/ESモジュールJSで、GitHub Pagesにそのまま公開できる
(公開手順は [`README.md`](README.md) を参照)。ローカルで動作確認する場合は、
`fetch`でDEMを取得するため簡易HTTPサーバーで配信する必要がある(`file://`では動かない)。

```
uv run python -m http.server 8000 --directory docs
```

ブラウザで `http://localhost:8000/` を開く。

### 手順1: DEMファイルを選択(「0. DEMファイルを選択」)

ページを開くと、同梱のサンプルDEM(震災前・震災後・豪雨後)3件が自動的に読み込まれ、
すぐに陰影図クリックへ進める。DEMを変更したい場合は以下を行う。

1. 各行のパス欄に、DEM(GeoTIFF)の相対パス(例: `data/xxx.tif`)または絶対URLを入力する。
2. 「+ DEMを追加」で行を増やせる。各行の「削除」ボタンで行を消せる(最低1行は必要)。
3. 各行の「陰影図に使う」ラジオボタンで、陰影図に使うDEMを1つ選ぶ。
4. 各行の「凡例名」欄に断面図の凡例に表示したい名前を入力できる(任意。空欄なら
   ファイル名になる)。
5. 「この設定で読み込む」を押すと、入力した内容で陰影図・断面図が読み込み直される
   (ページの再読み込みは不要)。

### 手順2: 陰影図クリック・断面図確認・CSVダウンロード

- 陰影図(canvas)上で**2点クリック**すると、側線が確定し、下の「2. 断面図」に
  その場で断面図が表示される。
- **3点目をクリック**すると選び直しになる。
- **Escキー**で選択をクリアできる。
- 断面図が表示されると「CSVダウンロード」ボタンが有効になり、押すとその時点の
  データがCSVとしてダウンロードされる(PNG保存はブラウザの画面キャプチャ機能などを
  利用する)。
- サンプリング間隔は画面上の「サンプリング間隔 (m)」欄で変更できる。

## 出力ファイルについて

- **CSV**: `dem, distance, x, y, z` の列を持つロング形式(DEMごとに行が積み重なる)。
  `dem`列は凡例名(未指定ならファイル名)、`distance`は開始点からの距離(m)、`x`/`y`は
  実座標(EPSG:6675)、`z`は標高(m、範囲外・nodataは空欄/NaN)。
- **PNG**: 開始点からの距離を横軸、標高を縦軸とした折れ線グラフ。DEMごとに色分けされ、
  凡例はグラフ本体と重ならないよう右側に配置される。日本語のタイトル・軸ラベル・凡例に
  対応している。

## 既知の制約・注意点

- 入力するDEM・座標は**EPSG:6675**である必要がある(異なるCRSのDEMを混ぜるとエラーになる)。
- 同梱のサンプルDEMは、震災前DEM(`bfeq_pref`、y: 101250〜156000)と
  震災後/豪雨後DEM(`afeq_mliti`/`afst`、y: 156000〜160500)がy=156000付近でしか
  重ならない。震災前後を比較する断面図を作るには、この重なる範囲付近で側線を選ぶ必要が
  ある(重ならない範囲を選ぶと、震災前DEMの断面が全区間NaNになる。これはツールの
  不具合ではなく想定通りの挙動)。
- Pythonローカル版のGUIウィンドウ(`dem-profile-pick` / `dem-profile-pick-gui`)は
  実際にマウスでクリックする対話操作が前提のため、ヘッドレス環境やCIでは実行できない。
  ローカルの対話的なターミナルで実行すること。
